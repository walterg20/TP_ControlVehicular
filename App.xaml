<Application x:Class="TP_ControlVehicular.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:TP_ControlVehicular">
    <Application.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>

        <ControlTemplate x:Key="ErrorBorderTemplate">
            <Border BorderBrush="#E74C3C" BorderThickness="1.5" CornerRadius="3">
                <AdornedElementPlaceholder/>
            </Border>
        </ControlTemplate>

        <Style TargetType="TextBox">
            <Setter Property="Validation.ErrorTemplate" Value="{StaticResource ErrorBorderTemplate}"/>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Validation.ErrorTemplate" Value="{StaticResource ErrorBorderTemplate}"/>
        </Style>

        <Style TargetType="DatePicker">
            <Setter Property="Validation.ErrorTemplate" Value="{StaticResource ErrorBorderTemplate}"/>
        </Style>
    </Application.Resources>
</Application>
