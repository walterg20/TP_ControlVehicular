<UserControl x:Class="TP_ControlVehicular.Presentacion.Taller.CtlTaller"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Barra de herramientas -->
        <StackPanel Orientation="Horizontal" Margin="5" Grid.Row="0">
            <TextBlock Text="Buscar:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBox x:Name="txtBusqueda" Width="200" VerticalContentAlignment="Center" TextChanged="TxtBusqueda_TextChanged"/>
            <Button Content="➕ Nuevo" Width="70" Height="25" Click="BtnNuevo_Click" Margin="5,0"/>
            <Button Content="✏️ Modificar" Width="80" Height="25" Click="BtnModificar_Click" Margin="5,0"/>
            <Button Content="🗑️ Baja" Width="60" Height="25" Click="BtnBaja_Click" Margin="5,0"/>
        </StackPanel>

        <!-- DataGrid de talleres -->
        <DataGrid x:Name="dgTalleres" AutoGenerateColumns="False" Margin="5" Grid.Row="1">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding IdTaller}" Width="80"/>
                <DataGridTextColumn Header="Nombre" Binding="{Binding Nombre}" Width="*"/>
                <DataGridTextColumn Header="Dirección" Binding="{Binding Direccion}" Width="*"/>
                <DataGridTextColumn Header="Teléfono" Binding="{Binding Telefono}" Width="*"/>
                <DataGridCheckBoxColumn Header="Activo" Binding="{Binding Activo}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>