<Window x:Class="TP_ControlVehicular.Presentacion.Servicio.FrmServicio"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Servicio" Height="250" Width="500">
    <Window.Resources>
        <Style TargetType="TextBox"><Setter Property="ToolTip" Value="{Binding (Validation.Errors)[0].ErrorContent, RelativeSource={RelativeSource Self}}"/></Style>
    </Window.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Datos del Servicio" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>
        <Grid Grid.Row="1" VerticalAlignment="Top">
            <Grid.ColumnDefinitions><ColumnDefinition Width="120"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
            <Grid.RowDefinitions><RowDefinition Height="35"/><RowDefinition Height="35"/><RowDefinition Height="35"/></Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Text="Nombre:" VerticalAlignment="Center"/><TextBox Grid.Row="0" Grid.Column="1" Height="23" Text="{Binding Nombre, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}"/>
            <TextBlock Grid.Row="1" Text="Precio:" VerticalAlignment="Center"/><TextBox Grid.Row="1" Grid.Column="1" Height="23" Text="{Binding Precio, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock Grid.Row="2" Text="Activo:" VerticalAlignment="Center"/><CheckBox Grid.Row="2" Grid.Column="1" Content="Sí" VerticalAlignment="Center" IsChecked="{Binding Activo, Mode=TwoWay}"/>
        </Grid>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Guardar" Width="80" Height="25" Margin="0,0,10,0" Command="{Binding RegistrarCommand}" IsDefault="True"/>
            <Button Content="Cancelar" Width="80" Height="25" Click="BtnCancelar_Click"/>
        </StackPanel>
    </Grid>
</Window>
